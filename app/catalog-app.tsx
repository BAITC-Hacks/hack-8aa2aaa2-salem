'use client';
import {api} from '@/lib/http-client';
import {useState,useEffect,useRef,useCallback} from 'react';
import {Search,ShoppingBag,ArrowUpRight,ArrowUp,Zap,MessageSquare,ShieldCheck,Package,ChevronRight,Lightbulb,Cable,Plug,LayoutGrid,Sparkles,Plus,LoaderCircle,RefreshCw,Trash2,Check,AlertTriangle,X,Download} from 'lucide-react';
import seed from '@/data/catalog.json';
import {normalize,searchProducts,RawProduct,Product} from '@/lib/catalog';
import {Sheet,SheetContent,SheetTitle,SheetDescription} from '@/components/ui/sheet';
import {Dialog,DialogContent,DialogTitle,DialogDescription} from '@/components/ui/dialog';
import {AlertDialog,AlertDialogContent,AlertDialogTitle,AlertDialogDescription,AlertDialogAction,AlertDialogCancel,AlertDialogFooter} from '@/components/ui/alert-dialog';
import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';
import {Table,TableBody,TableRow,TableCell} from '@/components/ui/table';
import {Skeleton} from '@/components/ui/skeleton';
import {Toaster,toast} from 'sonner';
const initialProducts=seed.items.map(p=>normalize(p as RawProduct));
const cats=[{name:'Все товары',icon:LayoutGrid},{name:'Электрооборудование',icon:Zap},{name:'Освещение',icon:Lightbulb},{name:'Кабель и провод',icon:Cable},{name:'Розетки и выключатели',icon:Plug}];
const money=(n:number)=>new Intl.NumberFormat('ru-RU',{maximumFractionDigits:2}).format(n)+' ₸';
type Cart={items:{product:Product;quantity:number}[];total:number;itemCount:number;url:string};
type Proposal={id:string;product:Product;quantity:number;total:number;expiresAt:string};
type Message={role:string;text:string;products?:Product[];cartLink?:string;mode?:string;steps?:{label:string;ok:boolean}[];suggestions?:string[];proposal?:Proposal;comparison?:Product[];sources?:{title:string;url:string;checkedAt:string}[]};
function ProductImage({product,detail=false}:{product:Product;detail?:boolean}){const[failed,setFailed]=useState(false);return product.image&&!failed?<img className={detail?'detail-image':undefined} src={product.image} alt={product.name} loading="lazy" onError={()=>setFailed(true)}/>:<div className={detail?'detail-image image-fallback':'image-fallback'}><Package size={48}/><span>Нет изображения</span></div>;}
export default function CatalogApp(){
 const [products,setProducts]=useState<Product[]>(initialProducts),[query,setQuery]=useState(''),[category,setCategory]=useState('Все товары'),[sort,setSort]=useState('catalog'),[visible,setVisible]=useState(12),[complete,setComplete]=useState(false),[loadingMore,setLoadingMore]=useState(false);
 const [selected,setSelected]=useState<Product|null>(null),[detailLoading,setDetailLoading]=useState(false),[detailReady,setDetailReady]=useState(false),[detailError,setDetailError]=useState(''),[quantity,setQuantity]=useState(1),[contextId,setContextId]=useState<string>();
 const [chatOpen,setChatOpen]=useState(false),[draft,setDraft]=useState(''),[messages,setMessages]=useState<Message[]>([]),[sending,setSending]=useState(false);
 const [agentMode,setAgentMode]=useState(false);
 const [ready,setReady]=useState(false),[sessionError,setSessionError]=useState(''),[cart,setCart]=useState<Cart>({items:[],total:0,itemCount:0,url:'/?view=cart'}),[cartOpen,setCartOpen]=useState(false),[proposal,setProposal]=useState<Proposal|null>(null),[busy,setBusy]=useState(false),[operationError,setOperationError]=useState(''),[removeId,setRemoveId]=useState<string|null>(null);
 const detailRequest=useRef(0);const messageBottom=useRef<HTMLDivElement>(null);
 const bootstrap=useCallback(async()=>{setSessionError('');try{const data=await api<{cart:Cart;messages:Message[];mode:string;catalog:{items:Product[];complete:boolean}}>('session',{});setAgentMode(data.mode==='agent');setCart(data.cart);setMessages(data.messages);setProducts(data.catalog.items);setComplete(data.catalog.complete);setReady(true);}catch(e){setSessionError((e as Error).message);}},[]);
 useEffect(()=>{void bootstrap();if(new URLSearchParams(location.search).get('view')==='cart')setCartOpen(true);},[bootstrap]);
 useEffect(()=>{setVisible(12);},[query,category]);
 useEffect(()=>{messageBottom.current?.scrollIntoView({behavior:'smooth',block:'nearest'});},[messages,sending,chatOpen]);
 const mergeProduct=(p:Product)=>setProducts(old=>old.some(x=>x.id===p.id)?old.map(x=>x.id===p.id?p:x):[...old,p]);
 async function openProduct(p:Product){const ticket=++detailRequest.current;setSelected(p);setContextId(p.id);setQuantity(1);setDetailLoading(true);setDetailReady(false);setDetailError('');try{const fresh=await api<Product>('products/'+p.id);if(ticket!==detailRequest.current)return;setSelected(fresh);mergeProduct(fresh);setDetailReady(true);}catch(e){if(ticket===detailRequest.current)setDetailError((e as Error).message);}finally{if(ticket===detailRequest.current)setDetailLoading(false);}}
 async function send(text=draft){if(!text.trim()||sending||!ready)return;setSending(true);const clean=text.trim();setDraft('');setMessages(m=>[...m,{role:'user',text:clean}]);try{const reply=await api<Omit<Message,'role'>>('chat',{text:clean,productId:contextId});setMessages(m=>[...m,{role:'assistant',...reply}]);if(reply.products?.length===1)setContextId(reply.products[0].id);reply.products?.forEach(mergeProduct);}catch(e){setMessages(m=>[...m,{role:'assistant',text:(e as Error).message}]);setDraft(clean);}finally{setSending(false);}}
 async function prepare(){if(!selected||!detailReady||busy)return;setBusy(true);setOperationError('');try{const p=await api<Proposal>('proposals',{productId:selected.id,quantity});setSelected(null);setProposal(p);}catch(e){setOperationError((e as Error).message);}finally{setBusy(false);}}
 async function confirm(){if(!proposal||busy)return;setBusy(true);setOperationError('');try{const r=await api<{cart:Cart}>('proposals/'+proposal.id+'/confirm',{confirmed:true});setCart(r.cart);setMessages(m=>[...m,{role:'assistant',text:'Подтверждено: '+proposal.quantity+' × '+proposal.product.name+' добавлено в тестовую корзину.',cartLink:'/?view=cart'}]);setProposal(null);setCartOpen(true);toast.success('Товар добавлен в тестовую корзину');}catch(e){setOperationError((e as Error).message);}finally{setBusy(false);}}
 async function remove(){if(!removeId)return;setBusy(true);try{setCart(await api<Cart>('cart/remove',{productId:removeId,confirmed:true}));setRemoveId(null);toast.success('Позиция удалена');}catch(e){toast.error((e as Error).message);}finally{setBusy(false);}}
 async function loadMore(){if(loadingMore)return;setLoadingMore(true);try{const data=await api<{items:Product[];complete:boolean}>('catalog/load',{});setProducts(data.items);setComplete(data.complete);toast.success(data.complete?'Каталог загружен полностью':'Следующая страница добавлена в поиск');}catch(e){toast.error((e as Error).message);}finally{setLoadingMore(false);}}
 function exportCart(){const csv='\uFEFFАртикул;Наименование;Количество;Цена (₸, допущение);Сумма\r\n'+cart.items.map(({product:p,quantity:q})=>[p.sku,p.name,q,p.price,p.price*q].map(v=>'"'+String(v).replaceAll('"','""')+'"').join(';')).join('\r\n');const url=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8;'}));const a=document.createElement('a');a.href=url;a.download='ekt-test-cart.csv';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
 useEffect(()=>{const mc=(document as unknown as {modelContext?:{registerTool:(tool:unknown,options:unknown)=>void}}).modelContext;if(!mc)return;const lifecycle=new AbortController();try{mc.registerTool({name:'search_ekt_catalog',description:'Поиск по загруженной выборке и изменение видимой строки поиска. Корзина не изменяется.',inputSchema:{type:'object',properties:{query:{type:'string',maxLength:200}},required:['query'],additionalProperties:false},annotations:{readOnlyHint:false,untrustedContentHint:true},execute:async(input:unknown)=>{const q=(input as {query?:unknown})?.query;if(typeof q!=='string'||q.length>200)throw new Error('query must be a string up to 200 characters');setQuery(q);setCategory('Все товары');await new Promise(requestAnimationFrame);return {items:searchProducts(products,q).slice(0,10).map(p=>({id:p.id,name:p.name,sku:p.sku})),scope:'loaded_sample'};}},{signal:lifecycle.signal});}catch{/* Browser tool support is optional. */}return()=>lifecycle.abort();},[products]);
 let filtered=searchProducts(products,query,category);if(sort==='low')filtered=[...filtered].sort((a,b)=>a.price-b.price);if(sort==='high')filtered=[...filtered].sort((a,b)=>b.price-a.price);
 const chat=<><div className="chat-head"><div className="assistant-mark"><Sparkles size={20}/></div><div><strong>Помощник ЭКТ</strong><p>{agentMode?'ИИ-агент · данные ekt.kz':'Поиск по каталогу · без ИИ'}</p></div><span className="beta">BETA</span></div><div className="chat-body" role="log" aria-live="polite">{messages.length===0?<><span className="chat-date">ВАШ КОНСУЛЬТАНТ</span><div className="welcome-icon"><Sparkles size={26}/></div><h2>Разберёмся<br/>с вашим запросом</h2><p className="muted">Найду товар, проверю наличие и помогу сравнить характеристики.</p><div className="suggestions">{['Найти автомат Legrand','Проверить артикул 027228','Как работает корзина?'].map(t=><button key={t} disabled={!ready} onClick={()=>void send(t)}>{t}<ArrowUpRight size={16}/></button>)}</div><div className="assistant-note"><ShieldCheck size={17}/><span>Добавление — только после вашего подтверждения.</span></div></>:messages.map((m,i)=><div className={'message '+m.role} key={i}><span className="message-author">{m.role==='user'?'Вы':'Помощник ЭКТ'}</span><p>{m.text}</p>{m.comparison&&<div className="agent-comparison"><Table><TableBody><TableRow><TableCell>Параметр</TableCell>{m.comparison.map(p=><TableCell key={p.id}>{p.manufacturerSku||p.sku}</TableCell>)}</TableRow>{[...new Set(m.comparison.flatMap(p=>p.attributes.map(a=>a.label)))].map(label=><TableRow key={label}><TableCell>{label}</TableCell>{m.comparison!.map(p=><TableCell key={p.id}>{p.attributes.find(a=>a.label===label)?.value||'Нет данных'}</TableCell>)}</TableRow>)}</TableBody></Table><small>Сравнение известных характеристик не подтверждает взаимозаменяемость.</small></div>}{m.sources?.map(source=><a className="text-button" key={source.url} href={source.url} target="_blank" rel="noreferrer">{source.title} · {source.checkedAt}</a>)}{m.steps&&m.steps.length>0&&<details className="agent-steps"><summary>Проверки агента ({m.steps.length})</summary>{m.steps.map((step,j)=><div key={j}>{step.ok?'✓':'!'} {step.label}{!step.ok&&' — не завершено'}</div>)}</details>}{m.proposal&&<button className="secondary" onClick={()=>{setProposal(m.proposal!);setOperationError('');}}>Проверить предложение: {m.proposal.quantity} × {m.proposal.product.name}</button>}{m.products?.map(p=><button className="chat-product" key={p.id} onClick={()=>void openProduct(p)}><span><b>{p.name}</b><small>{p.sku} · {money(p.price)}</small></span><ChevronRight size={18}/></button>)}{m.suggestions&&i===messages.length-1&&<div className="suggestions agent-suggestions">{m.suggestions.map(t=><button key={t} disabled={sending||!ready} onClick={()=>void send(t)}>{t}</button>)}</div>}{m.cartLink&&<button className="text-button" onClick={()=>setCartOpen(true)}>Открыть тестовую корзину →</button>}</div>)}{sending&&<div className="chat-pending"><LoaderCircle className="spin" size={16}/>Ищу и проверяю данные. Это может занять несколько секунд…</div>}<div ref={messageBottom}/></div><form className="chat-compose" onSubmit={e=>{e.preventDefault();void send();}}><div className="compose-box"><input value={draft} onChange={e=>setDraft(e.target.value)} aria-label="Сообщение помощнику" maxLength={2000} placeholder="Опишите задачу или укажите артикул…"/><button disabled={!ready||sending||!draft.trim()} aria-label="Отправить сообщение"><ArrowUp size={20}/></button></div><p>{agentMode?'Сообщения обрабатывает OpenAI · не отправляйте платёжные данные':'Поиск в выборке · данные ekt.kz'}</p></form></>;
 return <div className="app-shell"><Toaster position="top-center"/><header className="topbar"><a className="brand" href="/" aria-label="ЭКТ главная"><span className="brand-mark"><Zap fill="currentColor" size={24}/></span><b>ЭКТ<span>комплект</span></b></a><div className="top-divider"/><span className="top-caption">Электротехника для ваших проектов</span><a className="original-link" href="https://ekt.kz" target="_blank" rel="noreferrer">ekt.kz<ArrowUpRight size={15}/></a><button className="cart-btn" onClick={()=>setCartOpen(true)}><ShoppingBag size={18}/>Корзина<span>{cart.itemCount}</span></button></header><div className="prototype-bar"><span className="tiny-label">ПРОТОТИП</span>Реальный каталог · тестовая корзина без оформления заказа</div>{sessionError&&<div className="service-error" role="alert"><AlertTriangle size={17}/>{sessionError}<button onClick={()=>void bootstrap()}>Повторить подключение</button></div>}<div className="workspace"><main className="catalog"><div className="breadcrumb">Каталог <ChevronRight size={12}/> Подбор товаров</div><div className="catalog-title"><div><h1>Найдётся нужная деталь<span>.</span></h1><p>Ищите по названию, артикулу или спросите помощника.</p></div><span className="catalog-sign">01 / КАТАЛОГ</span></div><form className="searchbar" onSubmit={e=>{e.preventDefault();setVisible(12);}}><Search size={21}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Например, автомат Legrand или 027228" aria-label="Поиск по каталогу" maxLength={200}/>{query&&<button className="clear-search" type="button" onClick={()=>setQuery('')} aria-label="Очистить поиск"><X size={16}/></button>}<button className="primary" type="submit">Найти</button></form><nav className="categories" aria-label="Категории">{cats.map(({name,icon:Icon})=><button key={name} onClick={()=>setCategory(name)} aria-pressed={category===name} className={category===name?'active':''}><Icon size={17}/>{name}</button>)}</nav><div className="results-head"><div><h2>{category}</h2><span>{filtered.length} в выборке</span></div><Select value={sort} onValueChange={setSort}><SelectTrigger className="sort-control" aria-label="Сортировка"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="catalog">По каталогу</SelectItem><SelectItem value="low">Сначала дешевле</SelectItem><SelectItem value="high">Сначала дороже</SelectItem></SelectContent></Select></div><div className="product-grid">{filtered.slice(0,visible).map(p=><article className="product-card" key={p.id}><button className="product-image" onClick={()=>void openProduct(p)} aria-label={`Открыть ${p.name}`}><ProductImage product={p}/><span className="product-brand">{p.brand||'ЭКТ'}</span></button><div className="product-copy"><span className="sku">АРТ. {p.sku}</span><button className="product-name" onClick={()=>void openProduct(p)}>{p.name}</button><p className={p.conflict?'stock stock-warning':p.quantity&&p.quantity>0?'stock':'stock unknown'}>{p.conflict?'Есть расхождение в данных':p.quantity===null?'Наличие в карточке':p.quantity>0?`В выгрузке: ${p.quantity} шт.`:'В выгрузке нет в наличии'}</p><div className="product-bottom"><strong>{money(p.price)}</strong><button aria-label={`Выбрать ${p.name}`} onClick={()=>void openProduct(p)}><Plus size={20}/></button></div></div></article>)}</div>{!filtered.length&&<div className="empty-result"><Search size={32}/><h3>В этой выборке ничего не найдено</h3><p>Попробуйте артикул, короткое название или загрузите ещё товары.</p><button className="text-button" onClick={()=>{setQuery('');setCategory('Все товары');}}>Сбросить поиск</button></div>}<div className="load-actions">{visible<filtered.length&&<button className="secondary" onClick={()=>setVisible(n=>n+12)}>Показать ещё {Math.min(12,filtered.length-visible)} товаров</button>}{!complete&&<button className="text-button" disabled={!ready||loadingMore} onClick={()=>void loadMore()}>{loadingMore?<LoaderCircle size={16} className="spin"/>:<RefreshCw size={16}/>}Загрузить следующую страницу ekt.kz</button>}</div><footer className="catalog-footer"><span>{products.length} товаров · {complete?'полный каталог':'частичная выборка'}</span><span>Валюта ₸ — допущение прототипа. Данные обновляются в карточке.</span></footer></main><aside className="chat-panel">{chat}</aside></div><button className="mobile-chat primary" onClick={()=>setChatOpen(true)}><MessageSquare size={19}/>Спросить помощника</button>
 <Sheet open={chatOpen} onOpenChange={setChatOpen}><SheetContent className="mobile-chat-sheet"><SheetTitle className="sr-only">Помощник ЭКТ</SheetTitle><SheetDescription className="sr-only">Консультация по каталогу</SheetDescription>{chat}</SheetContent></Sheet>
 <Sheet open={!!selected} onOpenChange={v=>{if(!v){++detailRequest.current;setSelected(null);setOperationError('');}}}><SheetContent className="detail-sheet"><SheetTitle>Карточка товара</SheetTitle><SheetDescription>{detailLoading?'Обновляю данные ekt.kz…':detailReady?'Цена и остатки проверены сейчас':'Данные из сохранённой выборки'}</SheetDescription>{selected&&<><ProductImage key={selected.id} product={selected} detail/><span className="sku">АРТ. {selected.sku}</span><h2>{selected.name}</h2><strong className="detail-price">{money(selected.price)}</strong>{detailLoading&&<div className="detail-skeleton"><Skeleton className="h-5 w-full"/><Skeleton className="h-5 w-3/4"/></div>}{detailError&&<div className="warning" role="alert">{detailError}<button className="text-button" onClick={()=>void openProduct(selected)}>Повторить</button></div>}{selected.conflict&&<div className="warning"><AlertTriangle size={19}/>{selected.conflictText}</div>}<p>{selected.description||'Описание не предоставлено.'}</p>{selected.attributes.length>0&&<><h3 className="section-title">Характеристики</h3><Table><TableBody>{selected.attributes.map(a=><TableRow key={a.key}><TableCell className="attribute-label">{a.label}</TableCell><TableCell>{a.value}</TableCell></TableRow>)}</TableBody></Table></>}<h3 className="section-title">Остатки по складам</h3><p className="muted">Общий остаток по API: {selected.quantity??'не указан'}. Возможность отгрузки уточняется у магазина.</p><div className="store-list">{selected.stores.filter(s=>s.quantity>0).map(s=><div key={s.id}><span>{s.name}</span><b>{s.quantity}</b></div>)}{!selected.stores.some(s=>s.quantity>0)&&<p className="muted">Положительные остатки не указаны.</p>}</div><p className="muted">Ссылка на сертификат в проверенной карточке не предоставлена.</p><div className="detail-actions"><label htmlFor="quantity">Количество</label><input id="quantity" type="number" min="1" max="100000" step="1" value={Number.isNaN(quantity)?'':quantity} onChange={e=>setQuantity(e.target.valueAsNumber)}/><button className="primary" disabled={!ready||!detailReady||busy||selected.conflict||selected.hasOffers||!Number.isInteger(quantity)||quantity<1} onClick={()=>void prepare()}>{busy?<LoaderCircle size={16} className="spin"/>:<ShoppingBag size={16}/>}Подготовить добавление</button></div>{operationError&&<div className="warning" role="alert">{operationError}</div>}<p className="muted">Следующим шагом вы подтвердите товар и количество. Это тестовая корзина.</p><div className="detail-links"><button className="secondary" disabled={!detailReady||sending} onClick={()=>{setSelected(null);setChatOpen(true);void send('Найди аналог');}}>Подобрать кандидатов на замену</button><a href={selected.url} target="_blank" rel="noreferrer" className="text-button">Товар на ekt.kz<ArrowUpRight size={16}/></a></div></>}</SheetContent></Sheet>
 <Dialog open={!!proposal} onOpenChange={v=>{if(!v&&!busy){setProposal(null);setOperationError('');}}}><DialogContent className="proposal-dialog"><DialogTitle>Подтвердите добавление</DialogTitle><DialogDescription>Товар попадёт только в тестовую корзину. Заказ в магазине не создаётся.</DialogDescription>{proposal&&<><div className="proposal-product"><ProductImage product={proposal.product}/><div><strong>{proposal.product.name}</strong><p className="sku">АРТ. {proposal.product.sku}</p></div></div><div className="proposal-summary"><span>{proposal.quantity} шт. × {money(proposal.product.price)}</span><strong>{money(proposal.total)}</strong></div><p className="muted">Перед добавлением повторно проверим цену и остаток. Предложение действует 5 минут.</p>{operationError&&<div className="warning" role="alert">{operationError}</div>}<button className="primary" disabled={busy} onClick={()=>void confirm()}>{busy?<LoaderCircle size={17} className="spin"/>:<Check size={17}/>}Да, добавить {proposal.quantity} шт.</button><button className="text-button" disabled={busy} onClick={()=>{setProposal(null);setOperationError('');}}>Отмена</button></>}</DialogContent></Dialog>
 <Sheet open={cartOpen} onOpenChange={setCartOpen}><SheetContent className="detail-sheet cart-sheet"><SheetTitle>Тестовая корзина</SheetTitle><SheetDescription>Сохраняется на сервере в рамках вашей сессии. Не оформляет заказ на ekt.kz.</SheetDescription>{!ready?<p className="muted">{sessionError||'Подключаю корзину…'}</p>:cart.items.length?<><div className="cart-items">{cart.items.map(({product:p,quantity:q})=><div className="cart-item" key={p.id}><ProductImage product={p}/><div><button className="cart-product-name" onClick={()=>{setCartOpen(false);void openProduct(p);}}>{p.name}</button><p>{q} шт. × {money(p.price)}</p><strong>{money(q*p.price)}</strong></div><button className="icon-button" aria-label={`Удалить ${p.name}`} onClick={()=>setRemoveId(p.id)}><Trash2 size={17}/></button></div>)}</div><div className="cart-total"><span>Итого по сохранённым ценам</span><strong>{money(cart.total)}</strong></div><p className="muted">Корзина не резервирует товар. Цены и остатки магазина могут измениться.</p><button className="secondary" onClick={exportCart}><Download size={17}/>Скачать список CSV</button><a className="primary" href="https://ekt.kz" target="_blank" rel="noreferrer">Перейти на ekt.kz<ArrowUpRight size={16}/></a><p className="muted">Состав тестовой корзины на сайт магазина не переносится.</p></>:<div className="empty-result"><ShoppingBag size={38}/><h3>Пока пусто</h3><p>Выберите товар и подтвердите добавление.</p><button className="primary" onClick={()=>setCartOpen(false)}>Вернуться к каталогу</button></div>}</SheetContent></Sheet>
 <AlertDialog open={!!removeId} onOpenChange={v=>!v&&!busy&&setRemoveId(null)}><AlertDialogContent><AlertDialogTitle>Удалить позицию?</AlertDialogTitle><AlertDialogDescription>Товар будет удалён из тестовой корзины.</AlertDialogDescription><AlertDialogFooter><AlertDialogCancel disabled={busy}>Оставить</AlertDialogCancel><AlertDialogAction disabled={busy} onClick={e=>{e.preventDefault();void remove();}}>Удалить</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog>
 </div>;
}


